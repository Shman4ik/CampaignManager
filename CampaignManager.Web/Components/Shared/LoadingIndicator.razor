<div class="flex justify-center items-center p-4">
    <div class="p-4 text-center">
        <div
            class="inline-block animate-spin rounded-full h-8 w-8 border-4 border-solid border-blue-500 border-r-transparent"
            role="status">
            <span class="sr-only">Загрузка...</span>
        </div>
        <p class="mt-2">@(Message ?? "Загрузка...")</p>
    </div>
</div>

@code {
    [Parameter] public string Message { get; set; } = string.Empty;
}