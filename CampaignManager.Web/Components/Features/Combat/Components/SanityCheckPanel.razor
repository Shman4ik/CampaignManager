@using CampaignManager.Web.Components.Features.Combat.Model
@using CampaignManager.Web.Components.Features.Combat.Services

@inject CombatService CombatService

<div>
    <h4 class="text-base font-semibold text-gray-900 mb-3">Проверка рассудка</h4>

    <!-- Выбор цели -->
    <div class="mb-3">
        <label class="block text-xs font-medium text-gray-600 mb-1">Цель</label>
        <select class="border border-primary-300 rounded-md px-2 py-1.5 text-sm text-primary-700 bg-primary-50 w-full focus:outline-none focus:ring-1 focus:ring-accent-500/25 focus:border-accent-500 hover:border-primary-400 hover:bg-white transition-colors"
                value="@_selectedTargetId"
                @onchange="OnTargetChanged">
            <option value="">— Выберите —</option>
            @foreach (var c in AvailableTargets)
            {
                <option value="@c.Id">@c.Name (Рас. @c.CurrentSanity)</option>
            }
        </select>
    </div>

    @if (_selectedTarget != null)
    {
        <div class="mb-2 text-sm text-gray-600">
            Текущий рассудок: <span class="font-semibold text-gray-900">@_selectedTarget.CurrentSanity / @_selectedTarget.MaxSanity</span>
        </div>

        <!-- Потеря при успехе -->
        <div class="mb-3">
            <label class="block text-xs font-medium text-gray-600 mb-1">Потеря при успехе</label>
            <input type="text" @bind="_successLoss"
                   class="border border-primary-300 rounded-md px-2 py-1 text-sm text-primary-700 bg-primary-50 w-full focus:outline-none focus:ring-1 focus:ring-accent-500/25 focus:border-accent-500 hover:border-primary-400 hover:bg-white transition-colors"
                   placeholder="0, 1, 1D3" />
        </div>

        <!-- Потеря при неудаче -->
        <div class="mb-3">
            <label class="block text-xs font-medium text-gray-600 mb-1">Потеря при неудаче</label>
            <input type="text" @bind="_failureLoss"
                   class="border border-primary-300 rounded-md px-2 py-1 text-sm text-primary-700 bg-primary-50 w-full focus:outline-none focus:ring-1 focus:ring-accent-500/25 focus:border-accent-500 hover:border-primary-400 hover:bg-white transition-colors"
                   placeholder="1D6, 1D10, 2D6" />
        </div>

        @if (_pendingSanityResult != null)
        {
            <!-- Результат проверки -->
            <div class="bg-secondary-100 border border-secondary-200 rounded-md p-3 mb-3">
                <div class="text-sm font-semibold @(_pendingSanityResult.AttackerSuccessLevel >= SuccessLevel.RegularSuccess ? "text-success-700" : "text-error-700")">
                    Бросок: @_pendingSanityResult.AttackerRoll против @_pendingSanityResult.AttackerSkillValue
                    — @CombatService.GetSuccessLevelText(_pendingSanityResult.AttackerSuccessLevel)
                </div>
                <div class="text-sm text-gray-700 mt-1">
                    Потеря рассудка: <span class="font-bold">@_pendingSanityResult.SanityLoss</span>
                    @if (_pendingSanityResult.SanityFumble)
                    {
                        <span class="text-error-700"> — крах, максимум возможного</span>
                    }
                </div>
                <div class="text-sm text-gray-600">
                    Рассудок: @_pendingSanityResult.SanityBefore → <span class="font-semibold">@_pendingSanityResult.SanityAfter</span>
                </div>
                @if (_pendingSanityResult.SanityLostToday is { } lostToday)
                {
                    <div class="text-xs text-gray-500">За игровой день потеряно: @lostToday</div>
                }

                @if (_pendingSanityResult.IntelligenceRoll is { } intRoll)
                {
                    <div class="mt-2 rounded border border-secondary-200 bg-white p-2">
                        <div class="text-xs font-semibold text-gray-700">
                            Потеряно 5+ пунктов за раз — проверка ИНТ
                        </div>
                        <div class="text-sm text-gray-700">
                            @intRoll против @_pendingSanityResult.IntelligenceValue —
                            @if (_pendingSanityResult.TriggeredTemporaryInsanity == true)
                            {
                                <span class="font-semibold text-secondary-700">успех: сыщик осознал ужас</span>
                            }
                            else
                            {
                                <span class="font-semibold text-success-700">провал: рассудок отгородился от ужаса</span>
                            }
                        </div>
                        <div class="text-xs text-gray-500 mt-0.5">
                            По правилам безумие наступает именно при успехе проверки ИНТ (стр. 153).
                        </div>
                    </div>
                }

                @if (_pendingSanityResult.TriggeredTemporaryInsanity == true)
                {
                    <div class="text-sm font-bold text-secondary-700 mt-2">
                        Временное безумие на @_pendingSanityResult.TemporaryInsanityHours ч.
                    </div>
                }
                @if (_pendingSanityResult.TriggeredIndefiniteInsanity)
                {
                    <div class="text-sm font-bold text-secondary-800 mt-1">
                        Бессрочное безумие — за день потеряно не меньше ⅕ рассудка.
                    </div>
                }
                @if (_pendingSanityResult.TriggeredPermanentInsanity)
                {
                    <div class="text-sm font-bold text-error-800 mt-1">
                        Рассудок на нуле — неизлечимое безумие, персонаж выбывает из игры.
                    </div>
                }

                <div class="flex gap-2 mt-3">
                    <button type="button" class="cm-btn cm-btn-primary cm-btn-sm w-full" @onclick="ApplySanityResult"><span>Применить</span></button>
                    <button type="button" class="cm-btn cm-btn-secondary cm-btn-sm w-full" @onclick="CancelSanityResult"><span>Отменить</span></button>
                </div>
            </div>
        }
        else
        {
            <!-- Ручной ввод броска -->
            <div class="mb-3">
                <div class="flex items-center gap-2">
                    <label class="text-xs font-medium text-gray-600 w-32 shrink-0">Бросок d100</label>
                    <input type="number" min="1" max="100"
                           value="@_manualRoll"
                           @oninput="OnRollInput"
                           class="border border-primary-300 rounded-md px-2 py-1 text-sm w-20 focus:outline-none focus:ring-1 focus:ring-accent-500/25 focus:border-accent-500"
                           placeholder="1–100" />
                    <button @onclick="AutoRollSanity" class="text-xs px-2 py-1 rounded bg-gray-100 hover:bg-gray-200 text-gray-600 whitespace-nowrap"><i class="fa-solid fa-dice-d20 mr-1"></i>авто</button>
                </div>
                @if (_manualRoll.HasValue)
                {
                    var level = CombatService.CalculateSuccessLevel(_manualRoll.Value, _selectedTarget!.CurrentSanity);
                    <div class="mt-0.5 ml-32 text-xs font-medium @(level >= SuccessLevel.RegularSuccess ? "text-success-700" : "text-error-700")">
                        → @CombatService.GetSuccessLevelText(level)
                    </div>
                }
                <p class="mt-2 text-xs text-gray-400">
                    Бонусные и штрафные кости к проверкам Рассудка не применяются (стр. 152).
                </p>
            </div>

            <button type="button" class="cm-btn cm-btn-info w-full" @onclick="RollSanityCheck"><span>Проверка рассудка</span></button>
        }
    }
</div>

@code {
    [Parameter] public List<Combatant> Combatants { get; set; } = [];
    [Parameter] public EventCallback<CombatActionResult> OnSanityResolved { get; set; }

    private string _selectedTargetId = "";
    private Combatant? _selectedTarget;
    private string _successLoss = "0";
    private string _failureLoss = "1D6";
    private CombatActionResult? _pendingSanityResult;
    private int? _manualRoll;

    private IEnumerable<Combatant> AvailableTargets =>
        Combatants.Where(c => !c.IsDead);

    private void OnTargetChanged(ChangeEventArgs e)
    {
        _selectedTargetId = e.Value?.ToString() ?? "";
        _selectedTarget = Guid.TryParse(_selectedTargetId, out var id)
            ? Combatants.FirstOrDefault(c => c.Id == id) : null;
        _pendingSanityResult = null;
        _manualRoll = null;
    }

    private void OnRollInput(ChangeEventArgs e)
    {
        if (int.TryParse(e.Value?.ToString(), out var v))
            _manualRoll = Math.Clamp(v, 1, 100);
    }

    private void AutoRollSanity()
    {
        _manualRoll = CombatService.RollD100();
    }

    private void RollSanityCheck()
    {
        if (_selectedTarget == null) return;

        var setup = new SanityCheckSetup
        {
            TargetId = _selectedTarget.Id,
            CurrentSanity = _selectedTarget.CurrentSanity,
            SuccessLoss = _successLoss,
            FailureLoss = _failureLoss,
            ManualRoll = _manualRoll
        };

        _pendingSanityResult = CombatService.ResolveSanityCheck(setup);
        _manualRoll = null;
    }

    private async Task ApplySanityResult()
    {
        if (_pendingSanityResult == null) return;
        await OnSanityResolved.InvokeAsync(_pendingSanityResult);
        _pendingSanityResult = null;
    }

    private void CancelSanityResult()
    {
        _pendingSanityResult = null;
    }
}
