# Wiki Feature

Edit-history tracking, reused across other features' editable content.

## Key Services
- `WikiHistoryService(dbContextFactory, logger)`.

## Key Models
- `EditHistoryEntry : BaseDataBaseEntity`, `EditAction` — who changed what and when.
