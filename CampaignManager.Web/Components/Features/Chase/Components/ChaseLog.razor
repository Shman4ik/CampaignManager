@using CampaignManager.Web.Components.Features.Chase.Model
@using CampaignManager.Web.Components.Features.Combat.Services

<div class="bg-white rounded-md shadow-sm border border-gray-200 p-4 h-full">
    <h3 class="text-lg font-semibold text-gray-900 mb-3">Журнал погони</h3>

    <div class="max-h-[60vh] overflow-y-auto space-y-2 pr-1">
        @if (LogEntries.Count == 0)
        {
            <p class="text-sm text-gray-400 text-center py-4">Журнал пуст</p>
        }
        else
        {
            @foreach (var entry in LogEntries)
            {
                <div class="border-l-4 @GetBorderColor(entry) bg-gray-50 rounded-r-md p-3">
                    <div class="flex items-center gap-2 mb-1">
                        <span class="inline-block bg-primary-100 text-primary-800 text-xs font-medium rounded px-1.5 py-0.5">
                            Р.@entry.Round
                        </span>
                        <span class="text-xs text-gray-400">@entry.Timestamp.ToString("HH:mm:ss")</span>
                        <span class="text-xs font-medium @GetActionColor(entry)">@GetActionLabel(entry)</span>
                    </div>
                    <p class="text-sm text-gray-700 leading-snug">@entry.Summary</p>
                    @if (entry.BonusDiceUsed > 0)
                    {
                        <p class="text-xs text-blue-600 mt-0.5">Бонусных костей: @entry.BonusDiceUsed</p>
                    }
                    @if (entry.MovementActionsLost > 0)
                    {
                        <p class="text-xs text-red-600 mt-0.5">Потеряно действий: @entry.MovementActionsLost</p>
                    }
                </div>
            }
        }
    </div>
</div>

@code {
    [Parameter] public List<ChaseActionResult> LogEntries { get; set; } = [];

    private static string GetBorderColor(ChaseActionResult entry) => entry.ActionType switch
    {
        ChaseActionType.SpeedCheck => "border-l-indigo-400",
        ChaseActionType.MovementAction => "border-l-blue-400",
        ChaseActionType.HazardCheck => entry.IsSuccess ? "border-l-green-500" : "border-l-orange-500",
        ChaseActionType.BarrierCheck => entry.IsSuccess ? "border-l-green-500" : "border-l-red-400",
        ChaseActionType.BarrierDestroy => entry.IsSuccess ? "border-l-green-500" : "border-l-amber-500",
        ChaseActionType.MeleeAttack => entry.IsSuccess ? "border-l-red-500" : "border-l-gray-400",
        ChaseActionType.RangedAttack => entry.IsSuccess ? "border-l-red-500" : "border-l-gray-400",
        ChaseActionType.CombatManeuver => entry.IsSuccess ? "border-l-purple-500" : "border-l-gray-400",
        ChaseActionType.SkipAction => "border-l-gray-300",
        ChaseActionType.CaughtEvent => "border-l-red-700",
        ChaseActionType.EscapedEvent => "border-l-green-700",
        ChaseActionType.Other => "border-l-gray-400",
        _ => "border-l-gray-300"
    };

    private static string GetActionColor(ChaseActionResult entry) => entry.ActionType switch
    {
        ChaseActionType.SpeedCheck => "text-indigo-600",
        ChaseActionType.MovementAction => "text-blue-600",
        ChaseActionType.HazardCheck => "text-orange-600",
        ChaseActionType.BarrierCheck => "text-amber-600",
        ChaseActionType.BarrierDestroy => "text-amber-700",
        ChaseActionType.MeleeAttack => "text-red-600",
        ChaseActionType.RangedAttack => "text-red-600",
        ChaseActionType.CombatManeuver => "text-purple-600",
        ChaseActionType.SkipAction => "text-gray-500",
        ChaseActionType.CaughtEvent => "text-red-700",
        ChaseActionType.EscapedEvent => "text-green-700",
        ChaseActionType.Other => "text-gray-600",
        _ => "text-gray-600"
    };

    private static string GetActionLabel(ChaseActionResult entry) => entry.ActionType switch
    {
        ChaseActionType.SpeedCheck => "Проверка скорости",
        ChaseActionType.MovementAction => "Перемещение",
        ChaseActionType.HazardCheck => "Помеха",
        ChaseActionType.BarrierCheck => "Преграда",
        ChaseActionType.BarrierDestroy => "Разрушение",
        ChaseActionType.MeleeAttack => "Ближний бой",
        ChaseActionType.RangedAttack => "Стрельба",
        ChaseActionType.CombatManeuver => "Манёвр",
        ChaseActionType.SkipAction => "Пропуск",
        ChaseActionType.CaughtEvent => "Пойман!",
        ChaseActionType.EscapedEvent => "Сбежал!",
        ChaseActionType.Other => "Действие",
        _ => ""
    };
}
